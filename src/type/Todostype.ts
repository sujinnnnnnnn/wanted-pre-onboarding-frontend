export type TodoData = {
  id: number;
  todo: string;
  isCompleted: boolean;
};
export type CreateTodoData = {
  todo: string;
};
export type UpdateTodoData = {
  todo: string;
  isCompleted: boolean;
};
